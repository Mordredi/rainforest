<% @products.each do |product| %>
<ul>
  <li><%= link_to "#{product.name}", one_product_path(product.id) %></li>
  <li><%= product.description %></li>
  <li><%= product.formatted_price %></li>
</ul>
<% end %>